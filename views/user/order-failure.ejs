<% var title = "Payment Failed" %>

<!-- Enhanced failure page styles -->
<style>
.failure-container {
  max-width: 800px;
  margin: 0 auto;
  padding: 2rem 1rem;
}
.failure-card {
  background: white;
  border-radius: 16px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
  padding: 3rem 2rem;
  text-align: center;
}
.failure-icon {
  font-size: 4rem;
  margin-bottom: 1.5rem;
  animation: pulse 2s infinite;
}
@keyframes pulse {
  0% { opacity: 1; }
  50% { opacity: 0.7; }
  100% { opacity: 1; }
}
.failure-title {
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 1rem;
  color: #dc3545;
}
.failure-subtitle {
  font-size: 1.2rem;
  color: #6c757d;
  margin-bottom: 2rem;
}
.failure-details {
  background: #f8f9fa;
  border-radius: 12px;
  padding: 1.5rem;
  margin: 2rem 0;
  text-align: left;
}
.failure-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: center;
  margin-top: 2rem;
}
.failure-actions .btn {
  padding: 0.75rem 2rem;
  border-radius: 25px;
  font-weight: 600;
  text-decoration: none;
  transition: all 0.3s ease;
}
.retry-info {
  background: #fff3cd;
  border: 1px solid #ffeaa7;
  border-radius: 8px;
  padding: 1rem;
  margin: 1rem 0;
  color: #856404;
}
.cart-restoration {
  background: #d1ecf1;
  border: 1px solid #b8daff;
  border-radius: 8px;
  padding: 1rem;
  margin: 1rem 0;
  color: #0c5460;
}
.suggestions-list {
  text-align: left;
  margin: 1rem 0;
}
.suggestions-list li {
  padding: 0.5rem 0;
  border-bottom: 1px solid #eee;
}
.suggestions-list li:last-child {
  border-bottom: none;
}
.countdown-timer {
  font-size: 1.1rem;
  font-weight: bold;
  color: #007bff;
}
@media (max-width: 768px) {
  .failure-actions {
    flex-direction: column;
  }
  .failure-actions .btn {
    width: 100%;
  }
}
.retry-payment-btn {
  background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
  border: none;
  color: white;
  padding: 0.75rem 2rem;
  border-radius: 25px;
  font-weight: 600;
  transition: all 0.3s ease;
  cursor: pointer;
}
.retry-payment-btn:hover {
  background: linear-gradient(135deg, #0056b3 0%, #004085 100%);
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(0, 123, 255, 0.3);
}
.retry-payment-btn:disabled {
  background: #6c757d;
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
}
.payment-method-info {
  background: #e3f2fd;
  border-left: 4px solid #2196f3;
  padding: 1rem;
  margin: 1rem 0;
  border-radius: 8px;
}
</style>

<div class="failure-container">
  <div class="failure-card">
    <!-- Failure Icon and Header -->
    <div class="failure-icon">
      <i class="bi bi-exclamation-triangle text-warning" id="failure-icon"></i>
    </div>
    
    <h1 class="failure-title" id="failure-title">Payment Failed</h1>
    <p class="failure-subtitle" id="failure-subtitle">
      We encountered an issue processing your payment. Don't worry, no charges were made.
    </p>

    <!-- ✅ FIXED: Transaction Details with transactionId -->
    <% if (locals.transactionId) { %>
    <div class="failure-details">
      <strong>Transaction ID:</strong> #<%= transactionId %>
      <br>
      <strong>Time:</strong> <%= new Date().toLocaleString('en-IN') %>
    </div>
    <% } %>

    <!-- Dynamic Failure Information -->
    <div id="failure-info"></div>

    <!-- Cart Restoration Status -->
    <div id="cart-restoration" class="cart-restoration d-none">
      <i class="bi bi-check-circle text-success me-2"></i>
      <strong>Good News!</strong> Your cart items have been restored and are waiting for you.
    </div>

    <!-- Retry Information -->
    <div id="retry-info" class="retry-info d-none">
      <i class="bi bi-info-circle me-2"></i>
      <span id="retry-message">Click the retry button below to attempt payment again.</span>
      <div id="countdown-container" class="mt-2 d-none">
        <span>Retrying in: </span>
        <span class="countdown-timer" id="countdown-timer">5</span>
        <span> seconds</span>
      </div>
    </div>

    <!-- Suggested Actions -->
    <div id="suggestions-container" class="d-none">
      <h5>What you can do:</h5>
      <ul class="suggestions-list" id="suggestions-list"></ul>
    </div>

    <!-- Action Buttons -->
    <div class="failure-actions" id="action-buttons">
      <button id="retry-payment-btn" class="retry-payment-btn d-none">
        <i class="bi bi-arrow-clockwise me-2"></i>Retry Payment
      </button>
      
      <a href="/cart" class="btn btn-primary" id="cart-btn">
        <i class="bi bi-cart me-2"></i>Return to Cart
      </a>
      <a href="/checkout" class="btn btn-outline-success" id="checkout-btn">
        <i class="bi bi-arrow-clockwise me-2"></i>Try Again
      </a>
      <a href="/shop" class="btn btn-outline-secondary">
        <i class="bi bi-shop me-2"></i>Continue Shopping
      </a>
    </div>

    <!-- Help Section -->
    <div class="mt-4 pt-3 border-top">
      <p class="text-muted mb-2">Need help?</p>
      <div class="d-flex justify-content-center gap-3">
        <a href="/contact" class="text-decoration-none">
          <i class="bi bi-envelope me-1"></i>Contact Support
        </a>
        <a href="/help/payment-issues" class="text-decoration-none">
          <i class="bi bi-question-circle me-1"></i>Payment Help
        </a>
      </div>
    </div>
  </div>
</div>


<!--  Load Razorpay SDK for UPI retries -->
<script src="https://checkout.razorpay.com/v1/checkout.js"></script>

<!--  Load PayPal SDK for PayPal retries -->
<% if (locals.paypalClientId) { %>
<script src="https://www.paypal.com/sdk/js?client-id=<%= paypalClientId %>&disable-funding=credit,card"></script>
<% } %>

<!-- SweetAlert2 for enhanced notifications -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<!--  Enhanced JavaScript for Dynamic Failure Handling -->
<script>
let failureData = null;

document.addEventListener('DOMContentLoaded', function() {
  // Get failure details from URL parameters and sessionStorage
  const urlParams = new URLSearchParams(window.location.search);
  const sessionData = JSON.parse(sessionStorage.getItem('paymentFailureData') || '{}');
  
  failureData = {
    transactionId: urlParams.get('transactionId') || sessionData.transactionId,
    failureType: urlParams.get('type') || sessionData.failureType || 'unknown',
    reason: urlParams.get('reason') || sessionData.reason,
    canRetry: urlParams.get('canRetry') === 'true' || sessionData.canRetry === true,
    cartRestored: urlParams.get('cartRestored') === 'true' || sessionData.cartRestored === true,
    retryDelay: parseInt(urlParams.get('retryDelay') || sessionData.retryDelay || '0'),
    suggestedActions: JSON.parse(urlParams.get('actions') || JSON.stringify(sessionData.suggestedActions || []))
  };

  console.log('🚨 Failure page loaded with:', failureData);

  // Clear sessionStorage to prevent stale data
  sessionStorage.removeItem('paymentFailureData');

  // Update UI based on failure type
  updateFailureUI(failureData);


  // Add retry button functionality
  setupRetryButton();
});

function updateFailureUI(data) {
  const { failureType, canRetry, cartRestored, suggestedActions, transactionId } = data;
  
  const failureIcon = document.getElementById('failure-icon');
  const failureTitle = document.getElementById('failure-title');
  const failureSubtitle = document.getElementById('failure-subtitle');
  const retryBtn = document.getElementById('retry-payment-btn'); 
  const checkoutBtn = document.getElementById('checkout-btn');
  const cartRestorationDiv = document.getElementById('cart-restoration');
  const retryInfoDiv = document.getElementById('retry-info');
  const suggestionsContainer = document.getElementById('suggestions-container');
  const suggestionsList = document.getElementById('suggestions-list');

  // Update based on failure type
  switch (failureType) {
    case 'cancelled':
      failureIcon.className = 'bi bi-info-circle text-primary';
      failureTitle.textContent = 'Payment Cancelled';
      failureTitle.className = 'failure-title text-primary';
      failureSubtitle.textContent = 'You cancelled the payment. Your cart items are safe and ready when you are.';
      break;

    case 'network_error':
    case 'timeout':
      failureIcon.className = 'bi bi-wifi-off text-warning';
      failureTitle.textContent = 'Connection Issue';
      failureTitle.className = 'failure-title text-warning';
      failureSubtitle.textContent = 'We had trouble connecting to process your payment. Please check your connection and try again.';
      break;

    case 'insufficient_funds':
      failureIcon.className = 'bi bi-credit-card text-danger';
      failureTitle.textContent = 'Insufficient Funds';
      failureTitle.className = 'failure-title text-danger';
      failureSubtitle.textContent = 'Your payment method doesn\'t have sufficient funds for this transaction.';
      break;

    case 'card_declined':
      failureIcon.className = 'bi bi-credit-card-2-front text-danger';
      failureTitle.textContent = 'Card Declined';
      failureTitle.className = 'failure-title text-danger';
      failureSubtitle.textContent = 'Your card was declined. Please contact your bank or try a different payment method.';
      break;

    case 'technical_error':
    default:
      failureIcon.className = 'bi bi-exclamation-triangle text-warning';
      failureTitle.textContent = 'Payment Failed';
      failureTitle.className = 'failure-title text-danger';
      failureSubtitle.textContent = 'We encountered a technical issue processing your payment. Please try again.';
      break;
  }

  // Show/hide retry button based on capability
  if (canRetry && transactionId) {
    retryBtn.classList.remove('d-none');
    if (checkoutBtn) checkoutBtn.style.display = 'none';
  } else {
    retryBtn.style.display = 'none';
  }

  // Show cart restoration status
  if (cartRestored) {
    cartRestorationDiv.classList.remove('d-none');
  }

  // Show retry information
  if (canRetry) {
    retryInfoDiv.classList.remove('d-none');
    document.getElementById('retry-message').textContent = 'Click the retry button below to attempt payment again.'; 
  }

  // Show suggested actions
  if (suggestedActions && suggestedActions.length > 0) {
    suggestionsContainer.classList.remove('d-none');
    suggestionsList.innerHTML = '';
    
    suggestedActions.forEach(action => {
      const li = document.createElement('li');
      li.innerHTML = `<i class="bi bi-check-circle text-success me-2"></i>${action}`;
      suggestionsList.appendChild(li);
    });
  }
}

function setupRetryButton() {
  const retryBtn = document.getElementById('retry-payment-btn'); 
  if (!retryBtn || !failureData.canRetry || !failureData.transactionId) return;

  retryBtn.addEventListener('click', async function() {
    console.log('🔄 Retrying payment for transaction:', failureData.transactionId);
    
    // Show loading state
    this.disabled = true;
    const originalText = this.innerHTML;
    this.innerHTML = '<i class="bi bi-hourglass-split me-2"></i>Preparing Retry...';
    
    try {
      await retryPayment(failureData.transactionId);
    } catch (error) {
      console.error('❌ Error during payment retry:', error);
      
      // Restore button state
      this.disabled = false;
      this.innerHTML = originalText;
      
      if (typeof Swal !== 'undefined') {
        Swal.fire({
          icon: 'error',
          title: 'Retry Failed',
          text: 'Unable to retry payment. Please return to checkout.',
          confirmButtonColor: '#007bff'
        });
      }
    }
  });
}

async function retryPayment(transactionId) {
  try {
    console.log('🔍 Making retry request for:', transactionId);
    
    const response = await fetch(`/checkout/retry-payment/${transactionId}`, {
      method: 'POST',
      headers: { 
        'Content-Type': 'application/json',
        'X-Requested-With': 'XMLHttpRequest'
      },
      credentials: 'same-origin'
    });
    
    console.log('🔍 Retry response status:', response.status);
    
    if (!response.ok) {
      if (response.status === 403) {
        throw new Error('Authentication failed. Please refresh the page and try again.');
      }
      throw new Error(`Request failed with status ${response.status}`);
    }
    
    const data = await response.json();
    console.log('🔍 Retry response data:', data);
    
    if (!data.success) {
      throw new Error(data.message || 'Retry initiation failed');
    }
    
    //Handle different payment methods for retry
    switch (data.paymentMethod) {
      case 'upi':
        await retryRazorpayPayment(data);
        break;
      case 'paypal':
        await retryPayPalPayment(data);
        break;
      case 'wallet':
        await retryWalletPayment(data);
        break;
      default:
        window.location.href = '/checkout';
        break;
    }
    
  } catch (error) {
    console.error('❌ Error in retryPayment:', error);
    throw error;
  }
}

async function retryRazorpayPayment(data) {
  console.log('🔄 Opening Razorpay with data:', data);
  
  const options = {
    key: data.keyId,
    amount: data.amount,
    currency: data.currency || 'INR',
    name: 'LacedUp',
    description: `Retry Transaction ${data.transactionId}`,
    order_id: data.razorpayOrderId,
    handler: function(response) {
      console.log('✅ Razorpay retry payment successful:', response);
      
      fetch('/checkout/razorpay/verify-payment', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'same-origin',
        body: JSON.stringify({
          razorpay_payment_id: response.razorpay_payment_id,
          razorpay_order_id: response.razorpay_order_id,
          razorpay_signature: response.razorpay_signature,
          transactionId: data.transactionId
        })
      })
      .then(r => r.json())
      .then(result => {
        if (result.success) {
          window.location.href = result.redirectUrl;
        } else {
          throw new Error(result.message || 'Payment verification failed');
        }
      })
      .catch(err => {
        console.error('Payment verification error:', err);
        window.location.reload();
      });
    },
    modal: {
      ondismiss: function() {
        console.log('⚠️ Razorpay retry payment dismissed');
        window.location.reload();
      }
    }
  };

  if (typeof Razorpay === 'undefined') {
    throw new Error('Razorpay SDK not loaded');
  }

  const razorpay = new Razorpay(options);
  razorpay.on('payment.failed', function(response) {
    console.error('❌ Razorpay retry payment failed:', response);
    window.location.reload();
  });

  razorpay.open();
}

async function retryPayPalPayment(data) {
  console.log('🔄 Initiating PayPal retry with data:', data);
  
  // Check if PayPal SDK is loaded
  if (typeof paypal === 'undefined') {
    throw new Error('PayPal SDK not loaded');
  }
  
  try {
    // Create PayPal buttons for retry
    const retryContainer = document.createElement('div');
    retryContainer.id = 'paypal-retry-container';
    retryContainer.style.position = 'fixed';
    retryContainer.style.top = '-1000px'; // Hide it off-screen
    retryContainer.style.left = '-1000px';
    document.body.appendChild(retryContainer);
    
    // Initialize PayPal buttons for retry
    await paypal.Buttons({
      createOrder: async () => {
        console.log('🔄 Creating PayPal retry order...');
        
        // Call your backend to create a new PayPal order for retry
        const response = await fetch('/checkout/paypal/create-order', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'same-origin',
          body: JSON.stringify({ 
            transactionId: data.transactionId,
            isRetry: true 
          })
        });
        
        const orderData = await response.json();
        
        if (!orderData.success) {
          throw new Error(orderData.message || 'Failed to create PayPal retry order');
        }
        
        return orderData.orderID;
      },
      
      onApprove: async (approvalData) => {
        console.log('✅ PayPal retry approved:', approvalData);
        
        // Capture the retry payment
        const response = await fetch(`/checkout/paypal/capture/${approvalData.orderID}`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'same-origin',
          body: JSON.stringify({ 
            transactionId: data.transactionId,
            isRetry: true 
          })
        });
        
        const result = await response.json();
        
        if (result.success) {
          window.location.href = result.redirectUrl;
        } else {
          throw new Error(result.message || 'PayPal retry capture failed');
        }
      },
      
      onError: (err) => {
        console.error('❌ PayPal retry error:', err);
        window.location.reload(); // Reload failure page
      },
      
      onCancel: () => {
        console.log('⚠️ PayPal retry cancelled');
        window.location.reload(); // Reload failure page
      }
      
    }).render('#' + retryContainer.id);
    
    // ✅ IMPROVED: Better button finding and clicking logic
    setTimeout(() => {
      // Try multiple strategies to find and click PayPal button
      const strategies = [
        () => retryContainer.querySelector('[data-funding-source="paypal"]'),
        () => retryContainer.querySelector('div[role="button"]'),
        () => retryContainer.querySelector('button'),
        () => retryContainer.querySelector('[tabindex="0"]'),
        () => retryContainer.querySelector('div[data-testid*="paypal"]')
      ];
      
      let clicked = false;
      for (const strategy of strategies) {
        const element = strategy();
        if (element && !clicked) {
          console.log('🔄 Found PayPal button, clicking...');
          element.click();
          clicked = true;
          break;
        }
      }
      
      if (!clicked) {
        throw new Error('No PayPal button found to click');
      }
    }, 1000); // Give it time to render
    
  } catch (error) {
    console.error('❌ PayPal retry setup failed:', error);
    
    // Clean up the container
    const container = document.getElementById('paypal-retry-container');
    if (container) {
      container.remove();
    }
    
    // Fallback to redirect
    window.location.href = `/checkout?retry=paypal&transactionId=${data.transactionId}`;
  }
}


async function retryWalletPayment(data) {
  const response = await fetch('/checkout/retry-wallet-payment', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    credentials: 'same-origin',
    body: JSON.stringify({ transactionId: data.transactionId })
  });
  
  const result = await response.json();
  
  if (result.success) {
    window.location.href = result.redirectUrl;
  } else {
    throw new Error(result.message || 'Wallet payment retry failed');
  }
}

</script>
